apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teamcity-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "teamcity-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teamcity-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
